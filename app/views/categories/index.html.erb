<div class="categories--container">
    <% @categories.each do |category| %>
  <div class="category">
      <%=link_to category.name, category_path(category), class:"category" %>
      <%= category.user_flashcards.count %>
  </div>
    <% end %>
</div>
