<div id="message-<%= message.id %>">
  <small>
    <%# it needs to be refine %>
    <strong>
      <%= message.friend.user_1.first_name %>
    </strong>
    <i><%= message.created_at.strftime("%b %e at %l:%M %p") %></i>
  </small>
  <p><%= message.text %></p>
</div>
