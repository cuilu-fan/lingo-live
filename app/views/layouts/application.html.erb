<!DOCTYPE html>
<html>
  <head>
    <title>LingoLive</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= yield(:head) if content_for(:head) %>
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <link rel="manifest" href="<%= asset_path 'manifest.json' %>">
    <%= render 'shared/ios_pwa_links' %>
  </head>
  <body>
    <div class="page--fixed--container">
      <% if content_for :navbar %>
        <%= yield :navbar %>
      <% else %>
        <%= render "shared/navbar" if user_signed_in? %>
      <% end %>
      <%= render "shared/flashes" %>
      <div class="page--scroll--container <%= "page--scroll--container--padded" unless @hide_padding %> <%= @freeze_screen ? "chatroom--disable--scroll" : nil %>">
        <%= yield %>
      </div>
      <% if content_for :footer %>
      <%= yield :footer %>
      <% else %>
      <%= render "shared/footer" if user_signed_in? %>
      <% end %>
    </div>
  </body>
</html>
